# TODO list
Simple TODO list application using ASP .NET Core.

## Get Started
Make sure you have installed Entity Framework CLI tools (`dotnet tool install --global dotnet-ef`). After cloning this repository, run `dotnet ef database update` to initialize the local SQLite database. Solution should be ready for work.